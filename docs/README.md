# 文档目录

## STATISTICS.md

Kotlin 语法样本代码的统计信息文档，包含：

- 总体统计（文件数、行数、包数量等）
- 文件列表（按包组织）
- 语法元素统计（类、接口、函数等数量）
- 包导入关系（IMPORTS 关系）
- 覆盖的语法点清单
- CALLS 关系重点场景清单
- 详细文件列表（含行数）

该文档由 `tools/stats.py` 脚本自动生成。

## 重新生成统计文档

在项目根目录运行：

```bash
python3 tools/stats.py
```
